package io.github.ch000se.automap.ui.theme

import androidx.compose.ui.graphics.Color

/** Light primary color used by the sample Material theme in dark mode. */
val Purple80 = Color(0xFFD0BCFF)

/** Light secondary color used by the sample Material theme in dark mode. */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Light tertiary color used by the sample Material theme in dark mode. */
val Pink80 = Color(0xFFEFB8C8)

/** Dark primary color used by the sample Material theme in light mode. */
val Purple40 = Color(0xFF6650a4)

/** Dark secondary color used by the sample Material theme in light mode. */
val PurpleGrey40 = Color(0xFF625b71)

/** Dark tertiary color used by the sample Material theme in light mode. */
val Pink40 = Color(0xFF7D5260)
